import 'package:flutter/material.dart';

class NewEventPage extends StatelessWidget {
  const NewEventPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('New Event Page', style: TextStyle(fontSize: 24)),
    );
  }
}